databases:
  - name: chic-co-db
    plan: free
    databaseName: chic_co
    user: chic_co

services:
  - type: web
    name: chic-co-web
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DATABASE_URL
        fromDatabase:
          name: chic-co-db
          property: connectionString
